Tablas en uso:

- users
- employees
- cronograma
- vacation_records
- daily_plan
- weekly_plan
- change_log

Instalación nueva: ejecuta `schema.sql` en el SQL Editor de Neon.

Carga de datos: `python backend/scripts/generar_sql_neon.py --apply`.
